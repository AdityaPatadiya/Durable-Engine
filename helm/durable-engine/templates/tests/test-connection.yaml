apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "durable-engine.fullname" . }}-test-connection"
  labels:
    {{- include "durable-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "durable-engine.fullname" . }}:{{ .Values.service.healthPort }}/health']
  restartPolicy: Never
